import React from 'react'

const JSXRules = () => {
  return (
    <div>
        <h1>"JSX Rules"</h1>
        <p>
            <ol>Jsx must return a single root element</ol>
            <ol>Jsx must use a closing tag</ol>
            <ol>Jsx is written in JavaScript</ol>
            <ol>Jsx can be embedded in HTML</ol>
            <ol>Jsx are written using camelCase (e.g. className instead of class)</ol>
        </p>
    </div>
  )
}

export default JSXRules